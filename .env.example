# Base path for deployment (e.g., '/thesen_app/' for subdirectory deployment)
# Leave empty or set to '/' for root deployment
# During project setup, replace inq_web with your actual project name
VITE_BASE_PATH=inq_web

# API base url for backend endpoints used by frontend
VITE_API_URL=

# WebSocket URL for realtime events used by the Inquiry Board
# Examples:
#   ws://localhost:4000            (insecure, local development)
#   wss://host.example/ws         (secure, production)
# Leave empty in .env and provide the appropriate URL for your environment
VITE_WS_URL=
